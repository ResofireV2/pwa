import app from 'flarum/forum/app';

app.initializers.add('resofire-pwa', () => {
  // Forum-side initialisation (service worker registration, install prompts,
  // push notifications) will be added in Stages 5, 6, and 7.
});
